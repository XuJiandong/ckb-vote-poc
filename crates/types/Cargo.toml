[package]
name = "ckb-vote-types"
version = "0.1.0"
edition = "2024"

[features]

[dependencies]
ckb-gen-types = { version = "1.1.0", default-features = false }
molecule = { version = "0.9.2", default-features = false }

